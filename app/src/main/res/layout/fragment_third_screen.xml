<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="16dp"
    tools:context=".fragments.ThirdIntroSliderScreenFragment">

    <ImageView
        android:id="@+id/welcome_header"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:layout_marginTop="150dp"
        android:contentDescription="@string/welcome_to_notewave"
        android:src="@drawable/taking_notes" />

    <TextView
        android:id="@+id/organize_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/welcome_header"
        android:layout_centerHorizontal="true"
        android:fontFamily="@font/inter_semibold"
        android:gravity="center"
        android:padding="4dp"
        android:text="@string/organize_title"
        android:textSize="24sp" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/organize_title"
        android:layout_centerHorizontal="true"
        android:fontFamily="@font/inter_light"
        android:gravity="center"
        android:padding="4dp"
        android:text="@string/organize_description"
        android:textSize="16sp" />
</RelativeLayout>